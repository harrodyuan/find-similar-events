# docs

Put the README screenshot/GIF here.

1. Capture a run (the terminal output of `python pipeline_all.py`, or the
   `notebooks/explore_matches.ipynb` output table) and save it as **`docs/demo.png`**
   (or `demo.gif`).
2. In the top-level `README.md`, uncomment the two `<!-- ... img ... -->` lines
   just under the intro.

Keep it under ~1 MB so the repo stays light.
